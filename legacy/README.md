# 旧版 Flask 原型（已弃用）

此目录为早期单文件 Flask 版本，仅供参考。

**请使用项目根目录的 `frontend/` + `backend/` 新版本。**

启动旧版（不推荐）：

```powershell
pip install -r legacy/requirements.txt
# 需在根目录配置 config.json
python legacy/app.py
```
